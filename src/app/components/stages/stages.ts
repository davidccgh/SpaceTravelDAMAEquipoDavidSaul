import { Component } from '@angular/core';

@Component({
  selector: 'app-stages',
  standalone: true,
  imports: [],
  templateUrl: './stages.html',
  styleUrl: './stages.css',
})
export class Stages {
  // No se requiere lógica adicional por ahora
}
